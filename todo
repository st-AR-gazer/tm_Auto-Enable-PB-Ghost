finish the 'folder' part of indexing.
fix bug where when less than five runs are in the widget everything spasses out...
fix 'convert ghost to replay' that happens if there isn't a local ghost (this is so that we can still load things...)
